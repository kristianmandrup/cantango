class MainController < ApplicationController
  def index
    # use Api::User methods, fx Api::User

    # @user = User.create!(:name => "Stanislaw", :role => 'user')
    # @ability = CanTango::Ability.new(@user).can?(:read, Article)
  end
end
